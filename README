TODO:
Client and Server function on a PSK Basis.

Server requires the public key of a client for it to connect
That same client requires the server public key too, this keeps a higher level of security than a TLS-Like implementation.


INSTALL SERVER:

curl -fsSL https://raw.githubusercontent.com/EthanHoward/SDSN-25/refs/heads/main/install_server.sh | sudo bash

INSTALL CLIENT:

curl -fsSL https://raw.githubusercontent.com/EthanHoward/SDSN-25/refs/heads/main/install_client.sh | sudo bash
